import { prisma } from "@/lib/prisma";import { getServerSession } from "next-auth";import { authOptions } from "@/lib/auth";import { redirect } from "next/navigation";import { PageHeader } from "@/components/ui/page-header";import { FadeIn } from "@/components/transitions";import { PaymentsClient } from "./client";export default async function OwnerPaymentsPage() {  const session = await getServerSession(authOptions);  if (!session?.user?.organizationId) redirect("/auth/login");  if (session.user.role !== "OWNER") redirect("/dashboard");  const payments = await prisma.paymentTransaction.findMany({    orderBy: { createdAt: "desc" },    include: { organization: { select: { name: true } }, plan: { select: { name: true } } },  });  const data = payments.map((p) => ({    id: p.id, organizationId: p.organizationId, planId: p.planId,    amount: Number(p.amount), currency: p.currency, type: p.type, status: p.status,    paymentMethod: p.paymentMethod, receiptUrl: p.receiptUrl, reason: p.reason,    subscriptionStart: p.subscriptionStart?.toISOString() ?? null,    subscriptionEnd: p.subscriptionEnd?.toISOString() ?? null,    notes: p.notes, createdAt: p.createdAt.toISOString(),    organization: { name: p.organization.name },    plan: { name: p.plan.name },  }));  return (    <FadeIn>      <div className="space-y-4">        <PageHeader title="Payments" description="Payment and renewal history" />        <PaymentsClient payments={data} />      </div>    </FadeIn>  );}