"use client";import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";import { Button } from "@/components/ui/button";import { Select } from "@/components/ui/select";import { Dialog } from "@/components/ui/dialog";import { Plus, Ticket } from "lucide-react";import { useState, useCallback } from "react";import { NewTicketForm } from "../_forms";type TicketInfo = { id: string; organizationId: string; subject: string; message: string; status: string; priority: string; createdBy: string; assignedTo: string | null; createdAt: string; organization: { name: string }; user: { name: string; email: string } | null };const statusColor: Record<string, string> = { OPEN: "text-blue-600 bg-blue-50", IN_PROGRESS: "text-amber-600 bg-amber-50", RESOLVED: "text-green-600 bg-green-50", CLOSED: "text-gray-400 bg-gray-100" };const priorityColor: Record<string, string> = { LOW: "text-gray-400", NORMAL: "text-blue-500", HIGH: "text-amber-500", URGENT: "text-red-500" };export function SupportClient({ tickets: initialTickets }: { tickets: TicketInfo[] }) {  const [tickets, setTickets] = useState(initialTickets);  const [ticketDialog, setTicketDialog] = useState(false);  const refresh = useCallback(async () => { const r = await fetch("/api/owner/tickets"); if (r.ok) setTickets(await r.json()); }, []);  async function updateStatus(id: string, status: string) {    await fetch(`/api/owner/tickets/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status }) });    refresh();  }  return (    <div>      <div className="flex justify-end mb-3"><Button size="sm" onClick={() => setTicketDialog(true)}><Plus className="h-4 w-4 mr-1" />New Ticket</Button></div>      <Card>        <CardHeader><CardTitle className="text-sm"><Ticket className="h-4 w-4 inline-block mr-1" />Support Tickets ({tickets.length})</CardTitle></CardHeader>        <CardContent className="p-0">          <div className="overflow-x-auto">            <table className="w-full text-xs">              <thead><tr className="border-b text-left"><th className="p-3 font-medium text-gray-500">Subject</th><th className="p-3 font-medium text-gray-500">Organization</th><th className="p-3 font-medium text-gray-500">Priority</th><th className="p-3 font-medium text-gray-500">Status</th><th className="p-3 font-medium text-gray-500">Created</th></tr></thead>              <tbody>{tickets.length === 0 ? <tr><td colSpan={5} className="p-6 text-center text-gray-400">No tickets</td></tr> : tickets.map((t) => (                <tr key={t.id} className="border-b hover:bg-gray-50">                  <td className="p-3 font-medium">{t.subject}</td>                  <td className="p-3 text-gray-500">{t.organization.name}</td>                  <td className="p-3"><span className={`font-medium ${priorityColor[t.priority] ?? ""}`}>{t.priority}</span></td>                  <td className="p-3">                    <Select options={[{ value: "OPEN", label: "Open" }, { value: "IN_PROGRESS", label: "In Progress" }, { value: "RESOLVED", label: "Resolved" }, { value: "CLOSED", label: "Closed" }]} value={t.status} onChange={(e) => updateStatus(t.id, e.target.value)} />                  </td>                  <td className="p-3 text-gray-500">{new Date(t.createdAt).toLocaleDateString()}</td>                </tr>              ))}</tbody>            </table>          </div>        </CardContent>      </Card>      {ticketDialog && (        <Dialog open onClose={() => setTicketDialog(false)} title="New Support Ticket">          <NewTicketForm onClose={() => { setTicketDialog(false); refresh(); }} />        </Dialog>      )}    </div>  );}