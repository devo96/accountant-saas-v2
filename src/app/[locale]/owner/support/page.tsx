import { prisma } from "@/lib/prisma";import { getServerSession } from "next-auth";import { authOptions } from "@/lib/auth";import { redirect } from "next/navigation";import { PageHeader } from "@/components/ui/page-header";import { FadeIn } from "@/components/transitions";import { SupportClient } from "./client";export default async function OwnerSupportPage() {  const session = await getServerSession(authOptions);  if (!session?.user?.organizationId) redirect("/auth/login");  if (session.user.role !== "OWNER") redirect("/dashboard");  const tickets = await prisma.supportTicket.findMany({    orderBy: { createdAt: "desc" },    include: { organization: { select: { name: true } }, user: { select: { name: true, email: true } } },  });  const ticketsData = tickets.map((t) => ({    id: t.id, organizationId: t.organizationId, subject: t.subject, message: t.message,    status: t.status, priority: t.priority, createdBy: t.createdBy,    assignedTo: t.assignedTo, createdAt: t.createdAt.toISOString(),    organization: { name: t.organization.name },    user: t.user ? { name: t.user.name, email: t.user.email } : null,  }));  return (    <FadeIn>      <div className="space-y-4">        <PageHeader title="Support" description="Manage support tickets" />        <SupportClient tickets={ticketsData} />      </div>    </FadeIn>  );}